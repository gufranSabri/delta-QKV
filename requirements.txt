torch>=2.1
transformers>=4.40
datasets>=2.16
numpy>=1.24
scikit-learn>=1.3
pyyaml>=6.0
tqdm>=4.65
torchvision>=0.16      # only for model.backbone=resnet18
matplotlib>=3.7        # only for `main.py inspect`

accelerate>=2.20

# Testing
pytest>=7.0

# Optional: labeling.scheme=bleurt
# BLEURT is a TensorFlow model and is NOT installed by default. See
# src/label/bleurt.py for setup. The default exact_match scheme needs none of it.
#   git clone https://github.com/google-research/bleurt.git && pip install ./bleurt
